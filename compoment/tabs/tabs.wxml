<!--compoment/tabs/tabs.wxml-->
<view class="tabs">
    <view class="tabs_tittle">
    <view wx:for="{{tabs}}" wx:key="id" class="tittle_item {{item.isActive?'active':''}}" bindtap="hanldeItemTap" data-index="{{index}}">
    {{item.name}}
    </view>
    </view>
    <view class="tittle_content">内容</view>
</view>
